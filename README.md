# URL_Shortner_App_Advanced
A simple URL Shortener built with Flask, SQLAlchemy, and SQLite. Includes user authentication, a dashboard to manage links, and fixes for common issues like application context errors and schema mismatches.
